import React from 'react';
import ReactDOM from 'react-dom/client';
import TippkoenigPrototype from './TippkoenigPrototype';

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <TippkoenigPrototype />
  </React.StrictMode>
);